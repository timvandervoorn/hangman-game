import * as React from 'react'
import IntroPage from './introPage'

export default class introPageContainer extends React.PureComponent {
  render() {
    return (
      <div>
          <IntroPage />
      </div>
    )
  }
}